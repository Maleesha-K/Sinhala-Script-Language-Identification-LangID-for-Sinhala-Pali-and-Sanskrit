# Core dependencies for the data pipeline.
# Pinned to the versions the pipeline was built and verified against.

pandas==2.2.2
numpy==1.26.4
scikit-learn==1.8.0

# Only needed by scripts/data_loading.py, which downloads SiPaKosa
# from the Hugging Face Hub.
datasets==5.0.0
fastapi>=0.100.0
uvicorn>=0.23.0
streamlit>=1.25.0
pydantic>=2.0.0
joblib>=1.3.0
